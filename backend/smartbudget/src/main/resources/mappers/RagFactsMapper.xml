<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartbudget.rag.RagFactsMapper">

    <resultMap id="SelectSpendingResultMap" type="com.smartbudget.rag.RagFactsDTO$SelectSpendingRow">
        <result property="totalIncome" column="total_income"/>
        <result property="totalExpense" column="total_expense"/>
        <result property="netAmount" column="net_amount"/>
        <result property="transactionCount" column="transaction_count"/>
    </resultMap>

    <resultMap id="CategoryAmountResultMap" type="com.smartbudget.rag.RagFactsDTO$CategoryAmountRow">
        <result property="categoryName" column="category_name"/>
        <result property="amount" column="amount"/>
    </resultMap>

    <!-- 해당 월 소비/수입 집계 (SQL 집계, LLM은 계산하지 않음) -->
    <select id="selectSpendingByYearMonth" resultMap="SelectSpendingResultMap">
        SELECT
            COALESCE(SUM(CASE WHEN t.amount &gt; 0 THEN t.amount ELSE 0 END), 0) AS total_income,
            COALESCE(SUM(CASE WHEN t.amount &lt; 0 THEN ABS(t.amount) ELSE 0 END), 0) AS total_expense,
            COALESCE(SUM(t.amount), 0) AS net_amount,
            COUNT(*)::int AS transaction_count
        FROM projectdb.transactions t
        WHERE t.user_id = #{userId}
          AND to_char(t.tx_datetime, 'YYYYMM') = #{yearMonth}
    </select>

    <!-- 카테고리별 지출 합계 (amount &lt; 0, SQL 집계) -->
    <select id="selectCategoryExpensesByYearMonth" resultMap="CategoryAmountResultMap">
        SELECT
            COALESCE(c.name, '미분류') AS category_name,
            SUM(ABS(t.amount)) AS amount
        FROM projectdb.transactions t
        LEFT JOIN projectdb.categories c ON t.category_id = c.category_id
        WHERE t.user_id = #{userId}
          AND to_char(t.tx_datetime, 'YYYYMM') = #{yearMonth}
          AND t.amount &lt; 0
        GROUP BY c.name
        ORDER BY amount DESC
    </select>
</mapper>
